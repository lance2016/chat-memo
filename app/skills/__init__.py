"""Agent Skills：可下载、可安装的技能包。

一个技能 = 磁盘上的一个目录，里面必须有 `SKILL.md`（YAML frontmatter + 正文），
可以再带若干参考文件和脚本。渐进式披露分三层，和长期记忆是同一套思路：

    第 0 层  system prompt 里只有「名字 + 一句话说明」   ← 常驻，随技能数量增长
    第 1 层  `skill_read` 读 SKILL.md 正文               ← 判断相关之后才付出代价
    第 2 层  `skill_file` 读技能目录里的附带文件          ← 正文点名了才读

**技能是文档，不是可执行程序。** 这套后端没有沙箱也没有 bash 工具，
技能里带的脚本只会被当成文本读出来，永远不会被执行。见 `app/skills/tool.py`。
"""
